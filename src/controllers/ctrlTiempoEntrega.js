const faker = require('faker')

module.exports = (() => {
    return {
        calcularTiempoEntrega() {
            let ranges = require('../utils/ranges.json')

            return ranges
        },
    }
})();
